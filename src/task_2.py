"""
Внимательно прочитайте код в этом файле и добавьте свой код везде, где встречается <YOUR CODE>.
Перед каждым <YOUR CODE> вы можете прочитать описание задания, помеченный фразой <TODO>.
для проверки введите из папки с проектом: pytest -v tests/test_task_2.py
"""


def simple_branch(left_value, right_value):
    """
    TODO: если left_value больше right_value, присвойте переменной result значение "верно", в ином случае - "неверно"
    """
    if "<YOUR CODE>":
        result = "<YOUR CODE>"
    else:
        result = "<YOUR CODE>"

    return result


def middle_branch(left_value):
    # TODO: если left_value равно 10, присвойте переменной result значение "равно", если больше - "больше",
    #  если меньше 10 - "меньше"
    result = "неправильно!!"

    if "<YOUR CODE>":
        "<YOUR CODE>"
    elif "<YOUR CODE>":
        "<YOUR CODE>"
    elif "<YOUR CODE>":
        "<YOUR CODE>"

    return result


def conditional_operation(value: bool, left_value: int, right_value: int):
    # TODO: если value имеет значение True, то складываем left_value и right_value, иначе вычитаем из left_value -
    #  right_value. Результат записываем в переменую result
    result = "неправильно!!"

    "<YOUR CODE>"

    return result


def checker(value: int):
    # TODO: если переменная value не типа int - присвоить переменной result значение False, иначе - True
    result = "неправильно!!"

    "<YOUR CODE>"
    result = True if result is not False else False
    return result


def hard_condition(left_value: int, right_value: int):
    """
    TODO: если переменная left_value находится в диапазоне [0, 10], а переменная right_value - в диапазоне [-10, 100],
     то присвоить переменной result значение True, иначе False
    """
    result = "неправильно!!"

    "<YOUR CODE>"

    return result
